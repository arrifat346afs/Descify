import { Button } from "@/components/ui/button"

export const BatchProsess = () => {
  return (
    <div className="flex justify-center items-center h-full">
      <Button variant={"ghost"}>Add a Folder</Button>
    </div>
  )
}

